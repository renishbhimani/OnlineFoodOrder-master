@model IEnumerable<OnlineFoodOrder.Areas.Order.Models.OrderModel>

@{
    ViewData["Title"] = "Order List";
    Layout = "~/Views/Shared/Admin/_Layout.cshtml";
}

<div class="page-content-wrapper">
    <div class="portlet light bordered margin-top-10">
        <div class="portlet-title">
            <div class="caption">
                <i class="icon-directions font-green hide"></i>
                <span class="caption-subject bold font-dark uppercase">Order List</span>
            </div>
        </div>
    </div>
</div>

<div class="page-content-wrapper">
    <div class="portlet light bordered ">
        <div class="portlet-body" style="overflow-x :auto">
            @*<form asp-area="Order" asp-controller="Order" asp-action="PR_Order_SelectByCustomerName">
                <div class="row">
                    @if (ViewBag.CustomerList != null)
                    {
                        <div class="col-lg-3">
                            <div class="form-group">
                                <select class="form-control OrderDropDown" name="CustomerID" asp-items="@(new SelectList(ViewBag.CustomerList, "CustomerID", "CustomerName").ToList()) ">
                                    <option>Select Customer</option>
                                </select>
                            </div>
                        </div>
                    }
                    <div class="col-lg-3 d-inline">
                        <input type="submit" class="btn btn-primary" value="Search" />
                        <a asp-area="Order" asp-controller="Order" asp-action="Index" class="btn btn-danger">Clear Filter</a>
                    </div>
                </div>
            </form>*@
            <table class="table table-striped table-bordered table-hover table-header-fixed dataTable">
                <thead>
                    <tr class="">
                        <th>No.</th>
                        <th>Customer Name</th>
                        <th>Address</th>
                        <th>Mobile No.</th>
                        <th>Total Amount</th>
                        <th>Creation Date</th>
                        <th>Modification Date</th>
                    </tr>
                </thead>
                <tbody id="sample_2">
                    @{
                        int i = 1;
                    }
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@i</td>
                            <td>@item.CustomerID</td>
                            <td>@item.Address</td>
                            <td>@item.MobileNo</td>
                            <td>@item.TotalAmount</td>
                            <td>@item.CreationDate</td>
                            <td>@item.ModificationDate</td>
                            <td class="text-center d-flex" style="padding-right:0px;">
                                <a class="btn btn-info btn-xs" style="float:right;" asp-area="Order" asp-controller="Order" asp-action="Add" asp-route-orderId="@Convert.ToInt32(item.OrderID)"><i class="fa fa-pencil" style="font-size:7px"></i></a>
                            </td>
                        </tr>
                        i++;
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts{
}


